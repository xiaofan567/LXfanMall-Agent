# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.7

# LangChain v1 + LangGraph v1
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
langgraph>=1.0.0

# Configuration
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0

# HTTP Client
httpx>=0.27.0

# JWT
python-jose[cryptography]>=3.3.0

# ── Phase 2: RAG 知识库 ──
# 向量数据库
pymilvus>=2.4.0
# 文本分割
langchain-text-splitters>=0.3.0
# Redis 缓存（复用现有 localhost:6379，db2 隔离）
redis[hiredis]>=5.0.0

# ── Document Parsing ──
# PDF
pypdf>=4.0.0
# Word (.docx)
python-docx>=1.1.0
# Excel (.xlsx)
openpyxl>=3.1.0
# Encoding detection
chardet>=5.0.0

# Rate Limiting
slowapi>=0.1.9
# HTML parsing (deepdoc html_parser)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── NLP & Tokenization ──
# Chinese word segmentation (nlp/tokenizer.py)
jieba>=0.42
# Token counting for chunking (rag/chunker.py)
tiktoken>=0.7.0
